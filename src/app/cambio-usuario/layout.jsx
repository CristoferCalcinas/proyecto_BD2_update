export const metadata = {
  title: "Cambio de Usuario",
  description: "Cambio de Usuario para la administración de una base de datos",
};

export default function CambioUsuarioPageLayout({ children }) {
  return <div>{children}</div>;
}
